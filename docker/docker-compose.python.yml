services:
  python_app:
    build: ./python_image
    container_name: python_env
    restart: always
    volumes:
      - ../dashboard_docker:/usr/src/app
    working_dir: /usr/src/app
    ports:
      - "8050:8050"

volumes:
  db_data:
